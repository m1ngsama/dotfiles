# Picom only composites windows.  Application-specific whole-window opacity is
# deliberately disabled so terminal text stays opaque; Alacritty controls its
# own background transparency.
active-opacity = 1.0;

inactive-opacity = 1.0;

frame-opacity = 1.0;
